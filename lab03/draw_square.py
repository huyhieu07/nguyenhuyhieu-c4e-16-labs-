from turtle import*
def drSquare(le, color):
    shape("turtle")

    pencolor(color)
    for i in range(4):
        forward(le)
        left(90)
    # mainloop()   #bo mainloop() khi lam ex4

# drSquare(100,"red")
